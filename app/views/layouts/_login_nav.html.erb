<%= link_to 'Logout', destroy_user_session_path, method: :delete, class: "btn btn-primary btn-sm" if @buttons.include?(1) %>
<%= link_to 'Home', profile_path, class: "btn btn-primary btn-sm"  if @buttons.include?(2) %>
<%= link_to 'Account Settings', edit_user_registration_path(current_user), class: "btn btn-primary btn-sm"  if @buttons.include?(3) %>
<%= link_to 'Sign Up', new_user_registration_path, class: "btn btn-primary"  if @buttons.include?(4) %>
<%= link_to 'Sign In', new_user_session_path, class: "btn btn-primary"  if @buttons.include?(5) %>
